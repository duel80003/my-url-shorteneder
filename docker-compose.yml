version: "3"

services:
  shorteneder:
    restart: always
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - app_redis
    env_file:
      - configs/.env
  app_redis:
    image: "redis"
    ports:
      - "6379:6379"
